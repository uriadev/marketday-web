---
interface Props {
	class?: string;
}

const { class: className = 'text-brand' } = Astro.props;
---

<div class:list={['mb-3.5 font-mono text-xs font-semibold tracking-[0.14em] uppercase', className]}>
	<slot />
</div>
